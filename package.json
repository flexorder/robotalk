{
  "name": "vanilla-basic",
  "version": "1.0.0",
  "scripts": {
    "start": "sirv ./src public --cors --single --no-clear --port 8000",
    "build": "npx copyfiles -u 1 src/**/* dist"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1"
  }
}
